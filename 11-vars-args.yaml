- name: variables from cammand line
  hosts: local
  connection: local
  tasks:
  - name: print course info
    ansible.builtin.debug:
      msg: "hello, iam learning {{ COURSE }}, Duration is {{ DURATION }}, trainer is {{ TRAINER }}"
